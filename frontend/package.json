{
  "name": "tcm-agent-frontend",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "prebuild": "node -e \"process.exit(process.platform==='linux'&&process.arch==='x64'?0:1)\" && npm install --no-save @esbuild/linux-x64@0.21.5 @rollup/rollup-linux-x64-gnu@4.60.2 || true",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^5.4.10"
  },
  "optionalDependencies": {
    "@esbuild/linux-x64": "0.21.5",
    "@rollup/rollup-linux-x64-gnu": "4.60.2"
  }
}
